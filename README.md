# Realtime Document Editor — Client

Simple React client (created to match create-react-app structure) that connects to the server via Socket.IO.

- Main files: `src/App.js`, `src/Editor.js`, `src/style.css`

Usage:
1. npm install
2. npm start

Open `http://localhost:3000/` to create or open a document.
